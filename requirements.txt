# WUI v3 — dependencias Python
# ================================
# Instalar con: pip install -r requirements.txt

fastapi==0.136.3
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.1

# Auth
python-jose[cryptography]==3.3.0
bcrypt==4.3.0
passlib[bcrypt]==1.7.4
email-validator==2.3.0

# HTTP client (usado por llm_service para llamadas a APIs externas)
httpx==0.28.1

# Base de datos SQLite
aiosqlite==0.21.0

# Programador de tareas (respaldo automático)
APScheduler==3.11.0

# Variables de entorno
python-dotenv==1.2.2

# Facilidades
python-multipart==0.0.20
